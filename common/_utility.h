#pragma once

#include <string>
#include <vector>



#if 0
namespace VocT_utility
{
	bool is_file_exist(const std::string &filepath);
	std::vector<std::wstring> split_string(const wchar_t *line, const wchar_t *delim);
	int create_file(const std::string &filepath);
	bool verify_entry(std::wstring &word, std::wstring &trans);
}
#endif
